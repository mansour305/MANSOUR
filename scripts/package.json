{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed": "tsx ./src/seed.ts",
    "e2e-admin-login": "tsx ./src/e2e-admin-login.ts",
    "e2e-browser": "playwright test --config=playwright.config.ts",
    "production-readiness-gate": "tsx ./src/production-readiness-gate.ts",
    "production-readiness-live-smoke": "tsx ./src/production-readiness-live-smoke.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
