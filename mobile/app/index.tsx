/**
 * Home Screen — Main entry point for Mawaeedak Mobile App
 * 
 * This file serves as the Expo Router entry point.
 * It renders the MobileApp component with full functionality.
 */

import MobileApp from '../src/MobileApp';

export default function HomeScreen() {
  return <MobileApp />;
}
